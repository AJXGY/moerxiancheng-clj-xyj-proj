# 5.2.15 TP 补充任务进展

- 生成时间：2026-04-24T13:29:00+00:00
- 说明：本文件是 `5.2.15.md` 主线 `PP+MB` 之外的附加 `TP=2` 推理实验记录，不替代主线验收结果。

## 当前结论

当前 `5.2.15` 已补充 TP 推理测试入口和结果汇总入口，但本轮真实实测未完成。阻塞原因是系统层 `mthreads-gmi` 能看到两张 MTT S3000，然而 Python 侧 `torch_musa` 初始化失败，`torch.musa.is_available()` 返回 `False` 且 `torch.musa.device_count()` 返回 `0`，因此无法启动真实 MUSA TP 推理采样。

严谨判定：**TP 已接入，未完成实测，不可写为已通过。**

## A-F 补充状态

| 指标 | 状态 | 说明 |
| --- | --- | --- |
| A | 部分完成 | TP 补充脚本已接入，但当前 Python MUSA runtime 不可用 |
| B | 已完成 | 已准备 TP=2、MB=1/2/4 三组推理补充配置 |
| C | 未完成 | 真实 TP 推理采样被 `torch_musa` 初始化失败阻塞 |
| D | 未完成 | 预测入口已接入 `train-infer-estimation` 的 `torch_infer_mvp.py`，但需在实测产物生成后执行 |
| E | 未完成 | 尚无本轮 TP 实测/预测误差结果 |
| F | 未完成 | 尚不能判定 TP 补充是否 ≤20% |

## 已接入文件

- 配置文件：`tp_parallel_configs.json`
- 实测脚本：`benchmark_tp_infer_time.py`
- 预测脚本：`fit_tp_time_model.py`
- 汇总脚本：`summarize_tp_results.py`
- 一键脚本：`run_5215_tp_suite.sh`

## 本轮阻塞信息

```text
MUSA driver initialization failed
torch.musa.is_available() = False
torch.musa.device_count() = 0
```

系统层检查：

```text
mthreads-gmi 可见设备：MTT S3000 x 2
```

## 恢复后复线命令

```bash
cd /home/o_mabin/moerxiancheng-clj-xyj-proj/xyj/5.2.15
bash run_5215_tp_suite.sh
```
