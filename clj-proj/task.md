5.1.5摩尔线程架构上推理任务运行测试
（1）测试项名称 
摩尔线程架构上推理任务运行测试 
（2）测试项标识 
MTT-INFER-RUN-TEST 
（3）测试内容 
在英特尔 CPU+ 摩尔线程 GPU 硬件架构环境下，验证异构计算架构适配能力。运行大模型推理任务，确认系统能够正确识别硬件资源，任务能够正常启动、执行并完成，无报错中断，满足指标 1.2 关于异构计算架构适配寒武纪设备的要求。 
（4）测试方法 
A. 在搭载英特尔 CPU+ 摩尔线程 GPU 的服务器上配置模型推理所需依赖环境、驱动及框架，测试终端机通过千兆以太网交换机连接测试服务器。
B. 选取 LLama3.1-8B 模型及标准推理 benchmark 工具，准备测试数据，确认硬件资源可见（单卡、单机双卡）。 
C. 分别在单卡、单机双卡两种规模下启动推理任务，监控任务运行状态。 
D. 观察任务日志，确认无硬件识别错误、显存溢出或核心转储等异常信息。 	 
E. 记录任务是否正常运行结束，并输出推理结果。
F. 以任务成功完成且结果正确为判定标准，验证架构适配性。 



5.1.6摩尔线程架构上训练任务运行测试
（1）测试项名称 
摩尔线程架构上训练任务运行测试
（2）测试项标识 
MTT-TRAIN-RUN-TEST 
（3）测试内容 
在英特尔 CPU+ 摩尔线程 GPU 硬件架构环境下，验证异构计算架构适配能力。运行大模型训练任务（全量或 LoRA 微调），确认系统能够正确调度计算资源，任务能够正常启动、执行并完成，满足指标 1.2 关于异构计算架构适配寒武纪设备的要求。 
（4）测试方法 
A．在搭载英特尔 CPU+ 摩尔线程 GPU 的服务器上配置模型训练所需依赖环境、驱动及框架。 
B．选取 LLama3.1-8B 模型及标准训练脚本，配置优化策略（如 ZeRO-1、重计算），准备训练数据集。 
C．分别在单卡、单机双卡两种规模下启动训练任务，监控任务运行状态及显存占用。 
D．观察任务日志，确认梯度计算、参数更新正常，无硬件通信错误或训练崩溃。 
E．记录任务是否正常运行结束，并输出损失函数收敛曲线或 checkpoints。 
F．以任务成功完成且模型可保存为判定标准，验证架构适配性。 


5.1.11 摩尔线程架构上训练任务处理模型输出测试
（1）测试项名称
摩尔线程架构上训练任务处理模型输出测试
（2）测试项标识
MTT-TRAIN-MODEL-STRUCT-TEST
（3）测试内容
在英特尔 CPU+ 摩尔线程 GPU 硬件架构环境下，验证被测系统对大模型训练任务的处理模型构建能力。输入 LLama3.1-8B 训练任务配置，系统应能输出详细的执行模型，包含 CPU-GPU/NPU 之间的任务分配方式、GPU 多卡间或多机间的任务划分关系、并行方式（如数据并行、流水线并行）、Microbatch 的执行逻辑，并成功提取出任务执行的 DAG 图，构建出完整的运行模型。
（4）测试方法
采用配置分析与模型验证相结合的方法。通过被测系统输入训练任务描述与资源映射策略，运行处理模型构建工具，解析输出的模型文件与可视化界面，核对是否包含任务分配、划分关系、并行策略、Microbatch 逻辑及 DAG 依赖图。
（5）前提和约束
环境准备：被测系统已部署，建模引擎服务运行正常。
硬件资源：测试环境包含英特尔 CPU 与摩尔线程 GPU 集群（支持多卡配置）。
测试用例：已准备 LLama3.1-8B 模型训练任务配置文件（含并行策略、Batch Size 等参数）。
系统状态：机器正常供电，网络正常，已登录被测系统所在容器。
（6）测试步骤
A. 启动被测系统的任务级建模模块，确认服务状态正常。
B. 输入 LLama3.1-8B 训练任务描述文件，配置英特尔 CPU 与摩尔线程 GPU 的资源映射策略。
C. 执行处理模型构建指令，生成训练任务处理模型。
D. 查看模型输出结果，验证是否明确展示了 CPU 与 GPU/NPU 之间的任务分配方式。
E. 验证是否展示了 GPU 多卡间或多机间的任务划分关系（如张量切片、流水线阶段）。
F. 验证是否明确了并行方式（数据并行/模型并行）及 Microbatch 的执行逻辑。
G. 检查是否成功提取并展示了任务执行的 DAG 图（有向无环图）（含算子依赖与通信节点）。
H. 记录验证结果，确认模型结构与实际运行逻辑的一致性。

5.1.12摩尔线程架构上人工智能推理任务处理模型输出测试
（1）测试项名称 
摩尔线程架构上人工智能推理任务处理模型输出测试
（2）测试项标识 
MTT-INFER-MODEL-STRUCT-TEST
（3）测试内容 
在英特尔 CPU+ 寒武纪 NPU 硬件架构环境下，验证被测系统对大模型推理任务的处理模型构建能力。输入 LLama3.1-8B 推理任务配置，系统应能输出详细的执行模型，包含 CPU-GPU/NPU 之间的任务分配方式、GPU 多卡间或多机间的任务划分关系、并行方式、Microbatch（或连续 Batch）的执行逻辑，并成功提取出任务执行的 DAG 图，构建出完整的运行模型。
（4）测试方法 
采用配置分析与模型验证相结合的方法。通过被测系统输入推理任务描述与资源映射策略，运行处理模型构建工具，解析输出的模型文件与可视化界面，核对是否包含任务分配、划分关系、并行策略、执行逻辑及 DAG 依赖图。
（5）前提和约束
环境准备：被测系统已部署，建模引擎服务运行正常。
硬件资源：测试环境包含英特尔 CPU 与摩尔线程 GPU 集群（支持多卡配置）。
测试用例：已准备 LLama3.1-8B 模型推理任务配置文件（含并发请求、Batch Size 等参数）。
系统状态：机器正常供电，网络正常，已登录被测系统所在容器。
（8）测试步骤 
A.启动被测系统的任务级建模模块，确认服务状态正常。
B.输入 LLama3.1-8B 推理任务描述文件，配置英特尔 CPU 与摩尔线程 GPU 的资源映射策略。 
C.执行处理模型构建指令，生成推理任务处理模型。 
D.查看模型输出结果，验证是否明确展示了 CPU 与 GPU/NPU 之间的任务分配方式。 
E.验证是否展示了 GPU 多卡间或多机间的任务划分关系（如层分割、请求分发）。 
F.验证是否明确了并行方式及 Microbatch（或连续 Batch）的执行逻辑。 
G.检查是否成功提取并展示了任务执行的 DAG 图（含推理算子依赖与数据加载节点）。 
H. 记录验证结果，确认模型结构与实际运行逻辑的一致性。




5.2.3摩尔线程架构计算密集型算子空间维度建模测试
（1）测试项名称
摩尔线程架构计算密集型算子空间维度建模测试
（2）测试项标识
MTT-COMPUTE-OP-SPACE-TEST
（3）测试内容
在摩尔线程GPU硬件架构环境下，验证算子级空间维度模型对计算密集型算子的预测准确性。选取LLama3.1-8B模型中的典型计算密集型算子（如矩阵乘C = A * B），分别在单卡、单机两卡规模下记录实际执行时间，并与模型预测时间对比，误差不超过20%。
（4）测试方法
A．在搭载摩尔线程GPU的服务器上配置性能建模工具所需环境，测试终端机连接测试服务器。
B．从LLama3.1-8B模型中提取代表性计算密集型算子（如矩阵乘、卷积等），确定其输入尺寸、数据类型等参数。
C．分别在单卡、单机两卡两种并行规模下，运行实际算子执行（可使用摩尔线程MUSA或PyTorch微基准测试），记录每个算子的实际执行时间T_real（五次运行取平均值）。
D．使用任务响应时间分析工具的算子级空间维度模型，输入相同算子描述和并行配置，得到预测时间T_sim。
E．计算误差 = |T_real - T_sim| / T_real × 100%，记录每个算子在不同规模下的误差值。
F．以所有测试算子的误差均≤20%为判定标准，验证摩尔线程架构上计算密集型算子空间维度建模的准确性。


5.2.6摩尔线程架构访存密集型算子空间维度建模测试
（1）测试项名称
摩尔线程架构访存密集型算子空间维度建模测试
（2）测试项标识
MTT-MEM-OP-SPACE-TEST
（3）测试内容
在摩尔线程GPU硬件架构环境下，验证算子级空间维度模型对访存密集型算子的预测准确性。选取LLama3.1-8B模型中的典型访存密集型算子（如数据拷贝、张量重塑等），分别在单卡、单机两卡规模下记录实际执行时间，并与模型预测时间对比，误差不超过20%。
（4）测试方法
A．在搭载摩尔线程GPU的服务器上配置性能建模工具所需环境，测试终端机连接测试服务器。
B．从LLama3.1-8B模型中提取代表性访存密集型算子（如数据拷贝、切片、拼接等），确定其输入尺寸、数据类型等参数。
C．分别在单卡、单机两卡两种并行规模下，运行实际算子执行（可使用摩尔线程MUSA或PyTorch微基准测试），记录每个算子的实际执行时间T_real（五次运行取平均值）。
D．使用任务响应时间分析工具的算子级空间维度模型，输入相同算子描述和并行配置，得到预测时间T_sim。
E．计算误差 = |T_real - T_sim| / T_real × 100%，记录每个算子在不同规模下的误差值。
F．以所有测试算子的误差均≤20%为判定标准，验证摩尔线程架构上访存密集型算子空间维度建模的准确性。


5.2.9摩尔线程架构通信密集型算子空间维度建模测试
（1）测试项名称
摩尔线程架构通信密集型算子空间维度建模测试
（2）测试项标识
MTT-COMM-OP-SPACE-TEST
（3）测试内容
在摩尔线程GPU硬件架构环境下，验证算子级空间维度模型对通信密集型算子的预测准确性。选取平台支持的通信算子（如AllReduce、Send/Recv等），在单机两卡规模下记录实际执行时间，并与模型预测时间对比，误差不超过20%。
（4）测试方法
A．在搭载摩尔线程GPU的服务器上配置性能建模工具所需环境及摩尔线程通信库（如MCCL），测试终端机连接测试服务器。
B．针对平台支持的通信算子（如AllReduce、Send/Recv等），准备不同消息大小的测试数据。
C．在单机两卡规模下，运行实际通信算子（可使用摩尔线程提供的MCCL测试程序或定制微基准），记录每个算子在每种消息大小下的实际执行时间T_real（五次运行取平均值）。
D．使用任务响应时间分析工具的算子级空间维度模型，输入相同通信算子描述、消息大小和并行配置，得到预测时间T_sim。
E．计算误差 = |T_real - T_sim| / T_real × 100%，记录每个通信算子在各种配置下的误差值。
F．以所有测试算子的误差均≤20%为判定标准，验证摩尔线程架构上通信密集型算子空间维度建模的准确性。


5.2.15摩尔线程架构并行配置下推理任务时间维度测试
（1）测试项名称
摩尔线程架构并行配置下推理任务时间维度测试
（2）测试项标识
MTT-PARALLEL-INFER-TIME-TEST
（3）测试内容
在摩尔线程GPU硬件架构环境下，验证应用级时空双维度模型对推理任务在不同并行配置下的时间预测准确性。以LLama3.1-8B模型推理任务为例，调整流水线并行路数和微批次数量，在单机两卡规模下记录实际推理延迟，并与模拟时间对比，误差不超过20%。
（4）测试方法
A．在搭载摩尔线程GPU的服务器上配置性能建模工具所需环境、深度学习框架及推理脚本，测试终端机连接测试服务器。
B．准备LLama3.1-8B模型推理任务脚本，支持配置流水线并行路数（如1、2）和微批次数量（如1、2、4），固定输入请求大小。
C．在单机两卡规模下，组合不同的流水线并行路数和微批次数量（至少3种组合），运行实际推理任务，使用benchmark工具记录平均推理延迟T_real。
D．使用任务响应时间分析工具，输入相同模型描述、并行配置和硬件拓扑，模拟得到推理延迟T_sim。
E．计算误差 = |T_real - T_sim| / T_real × 100%，记录每种并行配置下的误差值。
F．以所有测试配置的误差均≤20%为判定标准，验证摩尔线程架构上推理任务时间维度建模的准确性。