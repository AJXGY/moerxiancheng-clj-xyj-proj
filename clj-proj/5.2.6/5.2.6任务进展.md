# 5.2.6任务进展

- 生成时间：2026-04-17T00:00:00+00:00
- 任务标识：MTT-MEM-OP-SPACE-TEST
- 任务名称：摩尔线程架构访存密集型算子空间维度建模测试

## 当前结论

本任务已完成访存密集型算子的空间维度建模验证。测试覆盖 `copy`、`slice`、`cat` 三类来自 `Llama3.1-8B` 推理链路的访存算子，在单卡与单机双卡规模下分别进行五次实测，并采用“同类算子小规模/大规模标定，中规模验证”的方式生成 `T_sim`。所有**验证点**误差均不超过 `20%`，因此本次结果按题面要求判定为 **通过**。

## A-F 指标完成情况

| 指标 | 状态 | 说明 |
| --- | --- | --- |
| A | 已完成 | 已在摩尔线程 GPU 服务器上准备建模环境并完成资源检查 |
| B | 已完成 | 已准备 `copy`、`slice`、`cat` 三类算子及多种消息规模测试数据 |
| C | 已完成 | 已完成单卡、单机双卡两种规模下的五次运行平均采样 |
| D | 已完成 | 已基于空间维度模型生成 `T_sim` |
| E | 已完成 | 已记录各规模误差值，并区分标定点与验证点 |
| F | 已完成 | 所有验证点误差均 ≤ `20%`，结果为通过 |

## 测试对象与方法

### 算子类别

- `copy`：Hidden State Device Copy
- `slice`：KV Cache Slice
- `cat`：Heads Concat

### 标定与验证策略

- 每类算子采用 3 个规模点
- 最小规模与最大规模：标定点
- 中间规模：验证点

本任务的严格判定口径是：**只用验证点判断模型准确性**。标定点出现 `0%` 误差是建模设计的一部分，已明确标识，不再混入最终准确性结论。

## 环境摘要

- 设备后端：`musa`
- 设备数量：2
- 设备名称：`MTT S3000`, `MTT S3000`
- 单卡平均模型带宽：`93.58 GB/s`
- 双卡平均模型带宽：`181.93 GB/s`

## 实测与预测结果

| 算子 | 点类型 | 单卡 T_real(ms) | 单卡 T_sim(ms) | 单卡误差 | 双卡 T_real(ms) | 双卡 T_sim(ms) | 双卡误差 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `op_cat_heads_8mb` | calibration | 0.344 | 0.344 | 0.00% | 0.285 | 0.285 | 0.00% |
| `op_cat_heads_16mb` | validation | 0.485 | 0.455 | 6.20% | 0.353 | 0.346 | 1.96% |
| `op_cat_heads_32mb` | calibration | 0.676 | 0.676 | 0.00% | 0.469 | 0.469 | 0.00% |
| `op_copy_hidden_64mb` | calibration | 1.034 | 1.034 | 0.00% | 0.620 | 0.620 | 0.00% |
| `op_copy_hidden_128mb` | validation | 1.899 | 1.891 | 0.38% | 1.039 | 1.047 | 0.77% |
| `op_copy_hidden_256mb` | calibration | 3.606 | 3.606 | 0.00% | 1.903 | 1.903 | 0.00% |
| `op_slice_kv_128mb` | calibration | 1.230 | 1.230 | 0.00% | 0.678 | 0.678 | 0.00% |
| `op_slice_kv_256mb` | validation | 2.267 | 2.289 | 0.96% | 1.218 | 1.210 | 0.70% |
| `op_slice_kv_512mb` | calibration | 4.408 | 4.408 | 0.00% | 2.273 | 2.273 | 0.00% |

## 验证点汇总

| 算子类别 | 单卡验证误差 | 双卡验证误差 | 判定 |
| --- | ---: | ---: | --- |
| `cat` | 6.20% | 1.96% | 通过 |
| `copy` | 0.38% | 0.77% | 通过 |
| `slice` | 0.96% | 0.70% | 通过 |

## 与题面步骤的对应关系

### A-B. 环境准备与访存算子选取

- 已完成环境准备。
- 已从 `Llama3.1-8B` 中抽取访存密集型算子并确定参数。

### C. 五次实测平均

- 单卡与双卡均执行五次实测。
- `T_real` 使用五次均值。

### D-E. 建模与误差计算

- 已对每个配置生成 `T_sim`。
- 已计算误差并保留全部结果。

### F. 通过标准

- 真正用于判定的是验证点。
- 当前所有验证点误差均 ≤ `20%`。

## 关键产物

- 实测数据：[benchmark_results.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.6/artifacts/20260415T101500Z/benchmark_results.json)
- 建模结果：[space_model_results.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.6/artifacts/20260415T101500Z/space_model_results.json)
- 图表总览：[5.2.6图表汇总.md](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.6/5.2.6图表汇总.md)
- 误差图：[error_compare.png](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.6/charts/error_compare.png)
- 时间图：[runtime_compare.png](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.6/charts/runtime_compare.png)
- 带宽图：[bandwidth_model.png](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.6/charts/bandwidth_model.png)

## 如何复线

```bash
cd /home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.6
bash run_526_suite.sh
```

复线后重点核对：

- `artifacts/<timestamp>/benchmark_results.json`
- `artifacts/<timestamp>/space_model_results.json`
- `charts/error_compare.png`
- `charts/runtime_compare.png`




## 结论

本任务已按更严格口径完成，`0%` 误差只出现在显式标定点，未再被混作验证结果。
