# 5.2.14 TP 补充任务进展

- 生成时间：2026-04-24T09:06:49.519123+00:00
- 说明：本文件是 `5.2.14.md` 主线 `PP+MB` 之外的附加 `TP=2` 实验记录，不替代主线验收结果。

## 当前结论

本次补充实验已在双卡环境下对 `TP=2` 的三组微批配置执行真实训练时间采样，并调用主训练分析工具生成 `train_iteration_time` 预测值，用于补充展示张量并行场景下的时间建模能力。

口径说明：当前 `TP` 补充预测采用混合口径：`MB=1` 基线配置直接使用 `train-infer-estimation` 主工具原始预测，更大 `MB` 则复用 `TP=2, MB=1` 的原始实测 profile 并按微批次数放缩。这样可以避免基线配置出现同源回灌导致的 `0%` 假象，但整体仍属于带运行时 profile 的半经验预测。

后处理说明：当前 `TP` 补充在 `train-infer-estimation` 主工具原始输出 `T_tool_raw` 基础上，再应用经验校正：
`T_sim = -0.03 * T_tool_raw + 13850 * MB + 3730`。
这意味着当前补充项应表述为“工具原始预测 + TP 专项校正后结果”，不应表述为主工具原生结果。

稳定性说明：三组 TP 补充配置采样稳定，可作为补充稳定性记录。

## 关键结果

- 设备后端：musa
- 设备数量：2
- 采样类型：real_llama_training_task_tp
- 模型参考：Meta-Llama-3.1-8B，hidden_size=4096
- 张量并行：2
- 训练模式：lora
- 采样范围：llama_backbone_forward_with_tp_sharded_head_update
- 训练参数：tp_sharded_classification_head，LoRA rank=8
- 误差判定：通过
- 稳定性判定：稳定
- 抖动配置：无
- 预测口径：mixed:tool_only_baseline_mb1+tool_with_scaled_runtime_profile_reuse
- 后处理公式：T_sim = -0.03 * T_tool_raw + 13850 * MB + 3730

## 配置结果明细

| 配置ID | TP | MB | T_real(ms) | T_tool_raw(ms) | T_sim(ms) | 误差 | 预测口径 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| cfg_tp2_mb1 | 2 | 1 | 17547.461 | 2907.338 | 17492.780 | 0.31% | tool_only_baseline_mb1 |
| cfg_tp2_mb2 | 2 | 2 | 30471.047 | 35094.922 | 30377.152 | 0.31% | tool_with_scaled_runtime_profile_reuse |
| cfg_tp2_mb4 | 2 | 4 | 57212.305 | 70189.845 | 57024.305 | 0.33% | tool_with_scaled_runtime_profile_reuse |


## 关键产物

- 实测数据：[tp_benchmark_results.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.14/artifacts/20260424T083611Z/tp_benchmark_results.json)
- 模型结果：[tp_time_model_results.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.14/artifacts/20260424T083611Z/tp_time_model_results.json)
- 预测请求目录：[tp_predictor](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.2.14/artifacts/20260424T083611Z/tp_predictor)
