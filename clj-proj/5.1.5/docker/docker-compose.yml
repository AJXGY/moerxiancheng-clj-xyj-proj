services:
  mtt-infer-test:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    working_dir: /workspace/5.1.5
    volumes:
      - ..:/workspace/5.1.5
      - ../../model:/workspace/model
    environment:
      PYTHONUNBUFFERED: "1"
    command:
      - --model-path
      - /workspace/model/Meta-Llama-3.1-8B
      - --dry-run
