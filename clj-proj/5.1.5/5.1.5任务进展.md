# 5.1.5任务进展

- 生成时间：2026-04-17T00:00:00+00:00
- 任务标识：MTT-INFER-RUN-TEST
- 任务名称：摩尔线程架构上推理任务运行测试

## 当前结论

本任务已在 Intel CPU + 摩尔线程 GPU 环境下完成单卡、单机双卡推理运行验证，`preflight`、单卡、双卡三条链路均产生了结构化产物。按题面 A-F 的要求，本次重点验证的是：

- 环境、驱动、框架与模型是否可用
- 单卡、双卡推理任务能否正常启动、执行并完成
- 日志中是否存在硬件识别错误、显存溢出或核心转储
- 是否能够输出真实推理结果并据此做结果正确性判定

本次判定口径为：任务完成且结果正确。这里的“结果正确”采用了**首个非空输出行严格匹配标准答案**的规则，因此可证明推理链路与答案命中是成立的；但部分响应在首行之后仍有冗余续写，这一点已在报告中如实保留，不再写成“整段输出完全干净”。

## A-F 指标完成情况

| 指标 | 状态 | 说明 |
| --- | --- | --- |
| A | 已完成 | 已完成 Intel CPU + 摩尔线程 GPU 服务器上的环境预检查，并保留 `preflight.json` |
| B | 已完成 | 已选取 `Meta-Llama-3.1-8B` 与 3 条标准化可校验 prompt，确认单卡、单机双卡资源可见 |
| C | 已完成 | 已分别执行单卡、单机双卡推理任务，并输出运行结果 |
| D | 已完成 | 已检查执行日志，未发现硬件识别错误、显存溢出或核心转储 |
| E | 已完成 | 已记录任务结束状态，并保留真实推理输出文件与汇总结果 |
| F | 已完成 | 以“任务成功完成 + 首个非空输出行正确”为判定标准，本次结果为通过 |

## 测试对象与执行方式

### 模型与输入

- 模型：`Meta-Llama-3.1-8B`
- Prompt 数量：3
- 校验方式：逐条比对响应首个非空输出行与标准答案
- 标准答案：
  - `prompt-001` -> `5`
  - `prompt-002` -> `False`
  - `prompt-003` -> `3`

### 执行规模

- 单卡：`musa:0`
- 双卡：`musa:0,musa:1`
- 执行入口：`run_515_suite.sh`

## 关键产物

- 预检查结果：[preflight.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.1.5/artifacts/20260415T111320Z/preflight/preflight.json)
- 单卡汇总：[summary.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.1.5/artifacts/20260415T111320Z/single/summary.json)
- 双卡汇总：[summary.json](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.1.5/artifacts/20260415T111320Z/dual/summary.json)
- 单卡输出：[outputs.jsonl](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.1.5/artifacts/20260415T111320Z/single/outputs.jsonl)
- 双卡输出：[outputs.jsonl](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.1.5/artifacts/20260415T111320Z/dual/outputs.jsonl)
- 图表总览：[5.1.5图表汇总.md](/home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.1.5/5.1.5图表汇总.md)

## 关键结果摘要

### 环境与资源可见性

- 后端：`musa`
- 卡数：2
- 设备：`MTT S3000`
- 模型路径已检查通过
- 单卡、双卡资源均可见

### 执行结果

| 规模 | 任务状态 | 校验命中 |
| --- | --- | --- |
| 单卡 | 成功结束 | 3 / 3 |
| 双卡 | 成功结束 | 3 / 3 |

### 输出摘录

| 规模 | Prompt ID | 响应首行 | 后续输出情况 | 判定 |
| --- | --- | --- | --- | --- |
| 单卡 | prompt-001 | 5 | 无明显冗余 | 通过 |
| 单卡 | prompt-002 | False | 后续重复了题干内容 | 通过 |
| 单卡 | prompt-003 | 3 | 后续存在明显冗余续写 | 通过 |
| 双卡 | prompt-001 | 5 | 无明显冗余 | 通过 |
| 双卡 | prompt-002 | False | 后续重复了题干内容 | 通过 |
| 双卡 | prompt-003 | 3 | 后续存在明显冗余续写 | 通过 |

## 与题面步骤的对应关系

### A. 环境配置与服务器联通

- 已通过 `preflight_check.py` 检查 Python 依赖、驱动、GPU 可见性、模型目录与基础运行条件。

### B. 模型与测试数据准备

- 已使用 `Meta-Llama-3.1-8B`。
- 已准备可严格校验的 3 条测试输入。

### C. 单卡 / 双卡推理启动与监控

- 已执行单卡任务。
- 已执行单机双卡任务。
- 已输出对应 `summary.json` 与 `outputs.jsonl`。

### D. 日志异常检查

- 本次报告对应运行中未记录硬件识别错误、显存溢出或核心转储。

### E. 结束状态与推理结果输出

- 已保留结构化输出文件。
- 已记录任务结束状态。
- 已保留真实推理响应文本。

### F. 结果正确性判定

- 本任务采用首个非空输出行进行严格比对。
- 单卡、双卡均为 `3/3` 命中，因此本次按题面判为通过。

## 风险与保留说明

- 当前结果可以证明“推理任务成功完成且答案命中”，但**不能证明模型整段输出无冗余文本**。
- 如果后续验收要求变成“输出必须完全纯净，仅返回答案”，则需要继续收紧 prompt 模板和后处理逻辑。
- 当前报告不再把“首行正确”夸写成“整段完全正确”。

## 如何复线

```bash
cd /home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/5.1.5
bash setup_musa_env.sh

export LD_LIBRARY_PATH=/home/o_mabin/.local/gfortran/usr/lib/x86_64-linux-gnu:/home/o_mabin/.local/openblas/usr/lib/x86_64-linux-gnu/openblas-pthread:/home/o_mabin/.local/musa_toolkits/musa_toolkits_4.2.0/lib:/home/o_mabin/.local/mudnn/mudnn/lib:/usr/local/musa/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}

bash run_515_suite.sh \
  --model-path /home/o_mabin/moerxiancheng-clj-xyj-proj/clj-proj/model/Meta-Llama-3.1-8B \
  --device-type musa \
  --single-device-ids 0 \
  --dual-device-ids 0,1
```

复线后重点核对：

- `artifacts/<timestamp>/preflight/preflight.json`
- `artifacts/<timestamp>/single/summary.json`
- `artifacts/<timestamp>/dual/summary.json`
- `artifacts/<timestamp>/single/outputs.jsonl`
- `artifacts/<timestamp>/dual/outputs.jsonl`

## 结论

按当前题面定义，本任务已经完成；但从更严格的软件工程口径看，`5.1.5` 仍保留“答案首行正确、整段可能冗余”的输出质量风险，这一点已完整披露。
